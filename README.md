# Face-Identification-Blockchain-Verification
Face → web search → re-encode &amp; score → SHA-256 → Sepolia → read-back verify. Every candidate audited on-chain, so the search itself is tamper-evident.
